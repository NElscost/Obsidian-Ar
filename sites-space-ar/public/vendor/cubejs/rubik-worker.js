importScripts("./cube.js","./solve.js");\nlet ready=false;\nself.onmessage=({data})=>{\n  const {id,facelets,algorithm,mode}=data;\n  try {\n    const cube=Cube.fromString(facelets);\n    if(algorithm) cube.move(algorithm);\n    if(mode==="state"){\n      self.postMessage({id,facelets:cube.asString()});\n      return;\n    }\n    if(!ready){\n      Cube.initSolver();\n      ready=true;\n    }\n    self.postMessage({id,solution:cube.solve(),facelets:cube.asString()});\n  } catch(error) {\n    self.postMessage({id,error:String(error&&error.message||error)});\n  }\n};\n